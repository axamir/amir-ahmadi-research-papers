# Transcript Evidence

The raw primary NeuroSapiens+ transcript was exported as plain text and cryptographically hashed locally.

**Primary transcript SHA-256:**

`43ae4b627dc57c1c5b2aed52ee53fad56586ed25a8ee2f11996a63c6a2c26082`

The original export is preserved outside Git history together with the Safari WebArchive and screen recording.

## Repository split

The transcript is being committed in byte-preserving numbered text parts to keep individual repository files manageable.

Current status:

- `primary-transcript-part-00.txt` — committed
- `primary-transcript-part-01.txt` — pending
- `primary-transcript-part-02.txt` — pending
- `primary-transcript-part-03.txt` — pending

Concatenating the parts in numeric order must reproduce the hashed original export exactly once all parts are committed.

## Supplemental record

The primary text export currently ends around the VAREL same-session retrieval stage. Later tests — MIREN, Mirror/Black-box tests, implementation-like Great Library claims, final human record, and Safari/Chrome replication — are indexed in `research/test-registry.md` and `research/master-research-ledger.md` and will receive a separate verbatim supplemental transcript file before release.

## Publication requirement

The final release should not be marked complete until:

1. all raw transcript parts are committed or deposited immutably;
2. the supplemental transcript is committed;
3. all referenced screenshots/video/WebArchive are deposited through a binary-appropriate archival route;
4. the final SHA-256 manifest is regenerated against the deposited artifacts.
