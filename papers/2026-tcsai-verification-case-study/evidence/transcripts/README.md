# Transcript Evidence

The raw primary NeuroSapiens+ transcript was exported as plain text and cryptographically hashed locally.

**Primary transcript SHA-256:**

`43ae4b627dc57c1c5b2aed52ee53fad56586ed25a8ee2f11996a63c6a2c26082`

The original export is preserved outside Git history together with the Safari WebArchive and screen recording.

## Repository split

The transcript has been committed in byte-preserving numbered text parts to keep individual repository files manageable.

Current status:

- `primary-transcript-part-00.txt` — committed
- `primary-transcript-part-01.txt` — committed
- `primary-transcript-part-02.txt` — committed
- `primary-transcript-part-03.txt` — committed
- `primary-transcript-part-04.txt` — committed
- `primary-transcript-part-05.txt` — committed
- `primary-transcript-part-06.txt` — committed

The split was generated from the original 63,618-byte export. The original export was verified locally as:

`SHA-256 43ae4b627dc57c1c5b2aed52ee53fad56586ed25a8ee2f11996a63c6a2c26082`

The numbered parts preserve the byte sequence and are intended to be concatenated in numeric order to reconstruct the primary export. Some part boundaries intentionally occur mid-word because the split is byte-oriented rather than editorial.

## Supplemental record

The primary text export ends around the VAREL same-session retrieval stage. Later tests — MIREN, Mirror/Black-box tests, implementation-like Great Library claims, final human record, final system responses, and Safari/Chrome replication — are indexed in `research/test-registry.md` and `research/master-research-ledger.md` and require a separate verbatim supplemental transcript before final release.

## Publication requirement

The final release should not be marked complete until:

1. the supplemental transcript is committed;
2. all referenced screenshots/video/WebArchive are deposited through a binary-appropriate archival route;
3. the final SHA-256 manifest is regenerated against the deposited artifacts;
4. the browser-replication raw text outputs are preserved separately and directly compared;
5. Tony's response/right-of-reply record is appended without altering the frozen primary dataset.
